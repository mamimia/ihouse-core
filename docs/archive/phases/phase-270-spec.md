# Phase 270 — E2E Admin & Properties Integration Test

**Status:** Closed
**Prerequisite:** Phase 269 (E2E Webhook Ingestion)
**Date Closed:** 2026-03-11

## Goal

Add direct async function call E2E tests for the admin (operator dashboard) and
properties CRUD API surface. CI-safe — no live DB, no staging required.

## Files

| File | Change |
|------|--------|
| `tests/test_admin_properties_e2e.py` | NEW — 29 tests, 6 groups |

## Test Groups

| Group | Tests | Description |
|-------|-------|-------------|
| A | 3 | `get_tenant_summary` — 200, summary keys, empty DB 200 |
| B | 3 | `get_admin_metrics` — 200, is dict, empty DB 200 |
| C | 3 | `get_admin_dlq` — 200, is dict, empty DB 200 |
| D | 3 | `get_provider_health` — 200, is dict, empty DB 200 |
| E | 3 | `get_booking_timeline` — 200/404 for missing, 200 with data, 200/404 ghost |
| F | 5 | `list_properties` (200, empty), `get_property` (200, 404), `create_property` (200/201) |

## Key Discoveries

1. `get_tenant_summary` accesses `updated_at` on booking rows — mock rows must include this field.
2. `create_property` requires `property_id` in the request body (not generated by the server).

## Result

**~6,161 tests pass, 13 skipped, 0 failures.**
