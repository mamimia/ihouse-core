[pytest]
pythonpath = src
testpaths = tests
addopts = -q --ignore=tests/invariants --ignore=tests/deprecated
python_files = test_*.py
filterwarnings =
    ignore::DeprecationWarning:jwt
    ignore:.*HMAC key.*:UserWarning
